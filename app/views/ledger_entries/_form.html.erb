<%= form_with(model: ledger_entry) do |form| %>
  <% if ledger_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ledger_entry.errors.count, "error") %> prohibited this ledger_entry from being saved:</h2>

      <ul>
        <% ledger_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cdt_code %>
    <%= form.number_field :cdt_code %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.number_field :patient_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :performed_by %>
    <%= form.text_field :performed_by %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
